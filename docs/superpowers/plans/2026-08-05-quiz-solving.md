# 문제 풀기 화면 — 구현 계획

- 날짜: 2026-08-05
- 설계 문서: `docs/superpowers/specs/2026-08-05-quiz-solving-design.md`
- 요청: 레고 "학습도우미 관련해서 mediaedu-quiz 확인해서 최대한 비슷하게 구현해봐"
- 상태: 착수 대기(레고 승인 후 시작)

## In scope / Out of scope

**In scope** — 만든 문제를 앱 안에서 **눌러서 푸는** 화면 한 벌.
문제집 고르기 · 즉시 채점(색·잠금) · 진행 통계 · 형식 탭 · 해설 접기 · 교재 원본 열기 ·
그 문제로 물어보기 · 틀린 것만 다시 · 이번 판 다시 풀기 · 맞음/틀림을 복습 기록에 반영.

**Out of scope(이번에 하지 않을 것)**
1. 웹 배포·HTML 생성 (cmdALL은 앱이다)
2. 비밀번호·접속 열쇠 관리 (mediaedu 고유 사정)
3. 문제 만드는 프롬프트·형식 규칙 손대기 (6형식 100문항은 그 시험 전용 규격)
4. **찍기 방지 검사**(보기 길이·정답 쏠림) — 만들 때 붙는 기능이라 분리, 2단계로 제안
5. 정답 번호 자동 재배치
6. 풀이 기록의 앱 재시작 후 복원(화면 표시 한정 — 복습 기록은 노트에 남으므로 손실 없음)
7. 통계·그래프·연속 학습일 (기존 학습도우미 Out of scope 2항 그대로)

## 조각 (각 조각 끝에 `swift test` 전량 통과 + 커밋)

### Q0 — 이미 있는 문제집 가져오기 (레고 요청, 2026-08-05 "저 사이트에 있는 문제 그대로 갈 순 없니?")

**실측(2026-08-05)** — 볼트 `…/12003_미디어교육사/100제/`에 문제집 md **50개**가 있고,
`quiz_md.parse_md`로 훑으니 **50개 전부 100문항 온전 · 총 5,000문항 · 교재 쪽 링크 누락 0건**.
교재 PDF(`미디어교육사 교재_개정판(2025년).pdf`)도 같은 폴더에 있어 원본 열기가 바로 이어진다.

새로 만들 필요가 없다. **가져오면 곧바로 5,000문항이 풀 거리가 된다** — 풀기 화면보다 먼저 한다.

원본 형식(그대로 두고 읽기만 한다):

```markdown
## A. 옳은 것 고르기 (1~18)

**1.** 커뮤니케이션에 대한 설명으로 옳은 것은?
① 반드시 사람과 사람 사이에서만 일어난다.
② 발화자와 수신자 사이에 의미를 주고받는 것이다.
…

# 정답 · 해설

**1. ②** 커뮤니케이션 = 의미를 주고받는 것. [[…교재….pdf#page=9|📖 p.009]]
```

| 파일 | 변경 |
|---|---|
| `Sources/Services/QuizImportParser.swift` | **신규**. 위 형식 → `[StudyQuestion]`. 섹션 머리글=`type`, `#page=NNN`→`loc`, 보기는 한 줄에 여러 개 붙어 있어도 나눈다 |
| `Sources/Services/StudyNoteWriter.swift` | 근거 인용이 없을 때 `> 근거:` 줄을 **생략**(가져온 문제엔 원문 발췌가 없다). 기존 동작 불변 |
| `Sources/App/AppState+QuizImport.swift` | **신규**. 폴더 고르기 → 미리보기(몇 문항 읽혔는지) → **확인 후** 학습 노트 생성. 원본 md는 읽기만 한다 |
| `Tests/CmdMDTests/QuizImportParserTests.swift` | **신규**. 한 줄에 붙은 보기 · `#`/`##` 섹션 머리글 혼용 · 한 줄에 해설 여러 개 · 쪽 링크 없음 · 문항 수 모자람 |

**원본은 건드리지 않는다.** 학습 노트는 새 파일로 만들고, 만들기 전에 몇 개가 읽혔는지 보여준다.

### Q1 — 본문을 문제로 되돌리는 파서 (화면 없음)

| 파일 | 변경 |
|---|---|
| `Sources/Services/StudyQuizParser.swift` | **신규**. `ParsedItem.body` → `QuizQuestion?`(발문·보기·정답번호·해설·근거). 못 읽으면 `nil` |
| `Sources/Models/QuizQuestion.swift` | **신규**. 화면이 쓰는 값 그릇 |
| `Tests/CmdMDTests/StudyQuizParserTests.swift` | **신규**. 세 갈래 판정(보기/OX/스스로 채점) · 정답 표기 4가지(`2` / `2) …` / `②` / 보기 문장 일치) · 보기 개수 2~5개 · `type` 없음 · 줄 순서가 뒤바뀐 본문 |

**실측 완료(2026-08-05)** — 레고님 볼트의 실제 노트(50문항)를 세었다: `mcq` 40 · `OX` 6 · `단답` 4.
**눌러서 풀 수 있는 문항 92%**, 정답 표기는 50개 전부 깔끔했다. 여기서 두 가지가 확정됐다 —
**OX 갈래를 반드시 넣는다**(12%), **보기는 4개**이므로 5지선다로 고정하지 않는다.

### Q2 — 조회 두 개 + 채점 경로 공통화

| 파일 | 변경 |
|---|---|
| `Sources/Services/StudyIndex.swift` | `questionNotes()`(문제 노트 목록: 경로·문항 수·오늘 몫·교재 경로) · `items(forNotePath:)` 추가 |
| `Sources/App/AppState+StudyReview.swift` | 파일 쓰기 부분을 `applyStudyGrade(item:outcome:)`으로 추출. **동작 변경 없음** |
| `Tests/CmdMDTests/StudyIndexTests.swift` | 두 조회 테스트 추가 |

### Q3 — 상태 (화면 없이 동작만)

| 파일 | 변경 |
|---|---|
| `Sources/App/AppState+StudyQuiz.swift` | **신규**. 문제집 열기·답 고르기(1회 잠금)·필터(전체/안 푼 것/틀린 것/형식별)·해설 펼침·이번 판 다시 풀기·닫기 |
| `Tests/CmdMDTests/AppStudyQuizStateTests.swift` | **신규**. 같은 문제 두 번 눌러도 채점 1회 · 맞음→`앎`/틀림→`모름` · 필터별 목록 · 다시 풀기가 노트를 건드리지 않음 |

### Q4 — 화면

| 파일 | 변경 |
|---|---|
| `Sources/Views/StudyQuizView.swift` | **신규**. 문제집 고르기 + 푸는 화면(상단 통계·진행 막대, 형식 탭, 문제 카드, 하단 바) |
| `Sources/Models/LibraryMode.swift` | `MainMode.quiz` 추가 |
| `Sources/Views/MainEditorView.swift` · 툴바 모드 선택기 | `quiz` 분기 + 아이콘(연필/체크 계열) |
| `Sources/App/AppState.swift` 등 | 진입점 배선(모드 선택기·커맨드 팔레트) |

### Q5 — 재점검 + 실측

- 이어붙임 결함 훑기(다른 모드로 갔다 오기·탭 전환·노트가 그 사이 바뀐 경우)
- 실제 문제 노트로 수동 확인 후 레고에게 실기 확인 요청

## 위험과 대비

| 위험 | 대비 |
|---|---|
| AI가 만든 정답 표기가 제각각 | 파서 5단계 + 실패 시 기존 "스스로 채점" 방식으로 대체(화면이 비지 않음) |
| 한 문제가 두 번 채점됨 | 답을 고르는 즉시 잠금(mediaedu와 동일) |
| 채점 도중 노트가 바뀜 | 기존 채점 경로의 "포기 + 안내" 그대로 |
| 복습 기록이 지워질까 봐 | "기록 초기화" 대신 **"이번 판 다시 풀기"**(화면 표시만 지움) |
