# cmdALL 다음에 할 것 (todolist)

> 코딩 세션에서 "다음에 뭘 집을까"를 보는 파일. 규칙은 `CLAUDE.md`, 기획·요구사항은 `CmdMD-fork_prd.md`, 끝난 일 기록은 `docs/worklog.md`.

**운영 규칙**

- 끝난 항목은 **여기서 지우고** `docs/worklog.md`에 기록한다. 완료 표시를 쌓지 않는다(쌓으면 두 번째 worklog가 된다).
- 항목은 **왜 / 어디 / 방향** 을 갖춘다. 맥락 없이 열어도 바로 착수할 수 있게.
- **나중** 칸은 착수 전 레고 확인이 필요하다. 임의로 시작하지 않는다.

---

## 지금

- **문제 풀기 화면(100제 5,000문항) — 만드는 중**:
  - **왜** — 레고님 볼트에 미디어교육사 100제(문제집 md 50개 = **5,000문항**)가 이미 있는데 앱에서 풀 자리가 없다. mediaedu-quiz 사이트처럼 **보기를 눌러 바로 채점**하고, 틀린 문제는 며칠 뒤 복습에 저절로 다시 나오게 한다. 레고 요청(2026-08-05) "저 웹사이트를 구현하는 것에 집중하자".
  - **어디** — 설계 `docs/superpowers/specs/2026-08-05-quiz-solving-design.md` · 계획 `docs/superpowers/plans/2026-08-05-quiz-solving.md`. **계획 문서의 조각 Q3부터 이어가면 된다.**
  - **끝난 것** — **Q0** `QuizImportParser`(원본 문제집 md 읽기, 커밋 `0fc79a1`) · **Q2** `QuizRecordNote`+`QuizRecord`(기록장, 커밋 `63c3ee8`). 실제 100제 50개로 원본 읽기→기록장→채점→다시 읽기 왕복 확인함(5,000문항 전부 파싱, 기록장 374KB).
  - **남은 것** — **Q3** 동작(`AppState+StudyQuiz`: 답 고르기 1회 잠금·필터·이번 판 다시 풀기) → **Q4** 화면(`StudyQuizView` + `MainMode.quiz` + 툴바 아이콘) → **Q5** 재점검·실기 확인.
  - **꼭 지킬 것** — **원본 문제집 md는 절대 건드리지 않는다.** 문제 내용은 화면에 띄울 때마다 원본에서 읽고, 기록장에는 문항 번호와 복습 상태만 남긴다(정답이 두 곳에 적히면 반드시 한쪽이 낡는다 — 2026-08-05에 실제로 74곳 어긋난 사고를 겪었다).
  - **Q1은 미뤘다** — 앱이 만든 학습 노트를 읽는 파서. 볼트의 카드·문제 노트를 전부 지워서 지금은 풀 대상이 0개다. 나중에 학습도우미로 문제를 새로 만들면 그때.
- **교재 진도 관리 — 레고 실기 확인 대기**: 2026-08-02 구현·테스트·재설치까지 완료. 툴바 모드 선택기에 **"진도"(책 아이콘)** 가 생겼다. ＋로 교재를 고르면 목차 미리보기가 뜨고(레고님 미디어교육사 교재는 26개 장이 자동으로 잡히는 것까지 실물로 확인함), "등록"을 눌러야 진도 노트가 만들어진다. 확인할 것: 목차가 제대로 잡히는지, **쪽 번호 맞추기(±)** 로 실제 쪽과 맞출 수 있는지, 장을 누르면 교재의 그 쪽이 열리는지, 읽음 체크가 되는지, 카드·문제를 만든 장이 "만듦"에 잡히는지, 목차 없는 PDF에서 "장 추가…"가 쓸 만한지. 확인되면 이 항목 지운다(배포는 아직 — 로컬 설치본만).
- **다듬기 A~D + 한글·워드 진도 연결 — 레고 실기 확인 대기**: 2026-08-02 구현·테스트(1,531개)·재설치까지 완료. 확인할 것 —
  - **A 복습 화면**: 숫자키 1·2·3으로 채점, 스페이스로 정답 보기, 잘못 누르면 **되돌리기**(⌘Z), 창이 넓어졌는지. ⚠️ **지금은 확인 불가** — 2026-08-05에 카드·문제 노트를 전부 지워(100제로 대체) 복습할 항목이 0개다. 확인하려면 학습도우미로 카드를 새로 만들어야 한다.
  - **B 학습도우미**: "만들기" 도는 동안 **취소** 버튼이 생기고 실제로 멈추는지, 조각이 여러 개면 "조각 2/5 만드는 중…" 진행 막대가 보이는지.
  - **C**: 툴바 모드 선택기에 **학습도우미(모자 아이콘)·복습(체크 아이콘)** 이 생겼다. 팝업이 아니라 화면 전체로 열리는지, 닫으면 파일 화면으로 돌아오는지.
  - **D 할일 간트차트**: 긴 제목에 마우스 올리면 전체가 보이는지, 실수로 체크한 할일을 **"방금 완료한 것 되돌리기"** 로 살릴 수 있는지, 막대 색(중요도)과 아래 색 안내가 이해되는지.
  - **한글·워드 진도**: 한글/워드 교재로 카드·문제를 **새로** 만들면 진도 화면 "만듦"에 그 장이 잡히는지(2026-08-02 이전에 만든 것은 위치 정보가 없어 안 붙는다 — 다시 만들어야 함).

## 나중 (착수 전 레고 확인 — 임의로 시작하지 말 것)

- **조각 E — 폴더 오갈 때 화면 전환 매끄럽게**: "먼저 다시 살펴보고 필요할 때만" 하기로 한 항목. 지금도 쓸 만하면 안 해도 된다.
- **뜻으로 찾기(의미 검색)**: codex 자문에서 나온 제안 6개 중 맨 마지막으로 미룬 것. 임베딩이 들어가면 지금의 "임베딩 없는 B안" 구조를 바꿔야 하므로 착수 전 반드시 상의.
- **HWPML(아주 옛날 한글 파일 형식) 원본 그대로 보기**: MS 오피스·HWP·HWPX는 전부 이미 됨(2026-07-30 레고 확인 완료). 남은 건 HWPML 하나뿐인데 요즘 거의 안 쓰이는 형식이라 변환 도구가 아예 못 읽는다 — 되게 하려면 훨씬 무거운 도구가 필요해 보류. 실사용에서 이 형식 파일이 실제로 나오면 그때 재검토.
- **정부지원사업 사건철 + 제출 전 검수관(codex 자문, 2026-08-01)**: codex(gpt-5.6-sol)가 새 발전 방향으로 제안 — 공고문·신청서·증빙자료를 "사건 한 건" 단위로 묶어 마감일·자격조건·필수제출물·작성상태를 한 화면에서 관리(사건철) + 공고문 요구사항과 실제 작성 서류를 대조해 서명누락·예산불일치·증빙누락·분량초과를 체크리스트로 보여줌(제출 전 검수관). codex 추천 순서는 사건철→검수관. 레고 판단(2026-08-01): 지금은 착수 안 하고 메모만 남김 — 요구사항 확인 인터뷰를 시작했으나 첫 질문에서 보류 결정. 착수하려면 처음부터 다시 확인 필요(어떤 서류가 사건철 안에 들어가는지, 마감일·체크리스트를 누가 입력하는지, 검수 기준을 앱이 자동 판단할지 등 전부 미정).
