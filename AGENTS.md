# cmdALL 문서 지도

작업 전에 아래를 순서대로 본다.

1. **`CLAUDE.md`** — 프로젝트 규칙·현재 상태. 지키지 않으면 안 되는 것들(비샌드박스 유지·kordoc/claude는 Process 호출·파일 변경은 제안→확인→실행·Phase 게이트·라이선스). "다시 꺼내지 말 것" 항목도 여기 있다.
2. **`docs/todolist.md`** — **다음에 할 것.** 무엇부터 집을지는 여기서 고른다. 지금 / 다음 / 나중으로 나뉘어 있다. **끝낸 항목은 지우고** worklog에 기록한다.
3. **`CmdMD-fork_prd.md`** — 기획·요구사항·스펙. 기능이 무엇이고 왜 필요한가. 진행 상태·완료 날짜는 여기 적지 않는다.
4. **`docs/worklog.md`** — 끝난 일의 날짜별 기록(append-only). 과거 결정의 근거를 찾을 때만 연다.

새 기능은 `docs/superpowers/specs/`(설계) → `docs/superpowers/plans/`(구현 계획) 순서로 문서를 남긴 뒤 착수한다.

`docs/archive/`는 역할이 끝난 문서 보관소다. 현재 상태의 근거로 삼지 말 것 — 그 시점 기록일 뿐이다.

문서를 새로 만들거나 옮기거나 정리할 때는 전역 `~/.claude/CLAUDE.md`의 「프로젝트 문서 구조」를 따른다.

## 이 저장소 특성

- **Swift Package(SPM) / macOS 14+ 네이티브 앱.** `swift test`가 게이트고, 현재 1,068개다.
- **kordoc(Node)·claude CLI는 외부 도구다.** 직접 구현하지 않고 `Process`로 부른다. 경로 탐지 실패 시 안내만 하고 크래시하지 않는다.
- **원본은 CmdMD(MIT) 포크.** 신규 기능은 가능하면 별도 파일·모듈로 분리해 업스트림 머지를 쉽게 둔다. Docufinder는 BSL 1.1이므로 **코드를 가져오지 않는다** — 아이디어만 독립 구현.
